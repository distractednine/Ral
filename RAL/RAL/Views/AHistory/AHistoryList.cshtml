@model List<RAL_DAL.Watcheda>

@{
    Layout = null;
}

@if (Model == null)
{
    <script type="text/javascript">window.location = '/InvalidURL';</script>
}
else
{
    if (Model.Count < 9)
    {
        <script type="text/javascript">setMainDivHeight();</script>
    }
    if (Model.Count == 0)
    {
        <script type="text/javascript">showEmptyListNotif();</script>
    }
    else
    {
        foreach (var wa in Model)
        {
            Html.RenderPartial("AHistoryTableRow", wa);
        }
    }
}
